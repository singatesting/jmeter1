machine:
  timezone:
    America/Los_Angeles
  java:
    version: openjdk7

general:
   artifacts:
     - $CIRCLE_TEST_REPORTS

test:
  post:
  - sh circle_jmeter.sh
  - VAR=$(cat projects/ABCD.xml | sed -ne '/<failure>true</failure>/s#\s*<[^>]*>\s*##gp');echo $VAR;if [ $VAR = "true" ] ;then exit 0; else exit 1 ;fi
