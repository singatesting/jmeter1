machine:
  timezone:
    America/Los_Angeles
  java:
    version: openjdk7

general:
   artifacts:
     - $CIRCLE_TEST_REPORTS

test:
  post:
  - sh circle_jmeter.sh
  - var=cat projects/ABCD.xml | grep -oPm1 "(?<=<failure>)[^<]+"
  - if [$var] ;then exit 0; else exit 1 ;fi

