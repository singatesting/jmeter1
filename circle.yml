machine:
  timezone:
    America/Los_Angeles
  java:
    version: openjdk7

general:
   artifacts:
     - $CIRCLE_TEST_REPORTS

test:
  post:
  - sh circle_jmeter.sh
  - cat projects/ABCD.jtl
  - java -jar jtl2junit.jar --input projects/ABCD.jtl --output junit.xml [--testSuiteName Hackathon]
